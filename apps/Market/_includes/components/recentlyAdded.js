
// TODO(millionz): figure out a non shitty way to do data fetching.
// For now I 've added recoil for simplicity but we need some kind
// of client for marketplace data 
// 
// figure out SSR to get rid of horrendus load times
export default function RecentlyAdded(props) {
	return(
		<div className="flex flex-col items-center justify-center">
			{/* do some magic with mapping */
			}
		</div>
	);
}